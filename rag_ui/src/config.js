export const SPINNER_THICKNESS = "4px";
export const DEBOUNCE_MS = 600; // ms for debounce
export const ANIMATION_DELAY = 120; // ms per word (slow)
export const ANIMATION_FAST_DELAY = 60; // ms per word (fast)

export const ACCEPTED_FILE_TYPES = [
  "application/pdf",
  "text/plain",
  "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
];

export const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5 MB
export const MAX_FILE_COUNT = 1; // Limit to 1 file
